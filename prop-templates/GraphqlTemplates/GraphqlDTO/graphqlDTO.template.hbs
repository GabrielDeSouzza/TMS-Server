

export class {{entityName}}GraphqlDTO {
  public static createInputToEntity(createInput: {{entityName}}Input) {
    return new {{entityName}}({

    });
  }

  public static updateInputToEntity(
    updateInput: {{entityName}}UpdateInput | undefined,
  ) {
    return updateInput
      ? new {{entityName}}({

        })
      : undefined;
  }
}
